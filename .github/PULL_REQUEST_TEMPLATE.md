## 這個 PR 改了什麼

## 這不是一份完成的練習解答

- [ ] 我確認這個 PR 沒有包含填好的 `solution.py`、`notes.md`、`ai_usage.md`（見 `CONTRIBUTING.md`）

## 檢查清單

- [ ] 有修改到的每個 `dayXX/`（或 `final_project/`）目錄下，`pytest -v` 通過
- [ ] 根目錄執行 `python check_submission.py` 沒有問題
- [ ] 如果修改了 `gate.py`／`check_submission.py`：`ruff check` 跟 `mypy` 通過
- [ ] 如果新增了一天：已同步更新根目錄 `README.md` 的天數清單、`CHECKLIST.md`（核心天數還需更新 `check_submission.py` 的 `range`）
- [ ] 如果修改了 `SPEC.md`／`AI_POLICY.md`／`RUBRIC.md`：已經有對應的 Issue 討論過，並在下面附上連結

## 相關 Issue

## AI 使用揭露（若適用）

如果這個 PR 的內容主要由 AI 生成，請說明你驗證過哪些部分、以及為什麼確認內容正確。
