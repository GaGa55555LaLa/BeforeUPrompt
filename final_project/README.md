# Store Price Index Reporter

這是 Day 13～14 的期末專案骨架，不包含解答。完整要求見 `PROJECT_SPEC.md`。

```bash
cd final_project
python -m pip install -r ../requirements-dev.txt
python -m price_report --help
pytest -v
```

預期執行：

```bash
python -m price_report \
  --results examples/results.csv \
  --config examples/config.toml \
  --output report.json
```

## 延伸知識
看 `CONCEPTS.md`（解題前或解題後皆可）。
